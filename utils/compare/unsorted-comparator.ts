import type { Comparator } from './default-comparator-by-options-computer'
import type { SortingNode } from '../../types/sorting-node'

export let unsortedComparator: Comparator<SortingNode> = () => 0
